/* 
SELECT BY ID
-- Query all columns (attributes) for every row in the CITY table.
*/

---
===================================================================
CODE BLOCK BELOW
===================================================================
---

SET NOCOUNT ON;
SELECT *
FROM city
WHERE id = 1661

go